Tags: [[__Machine_Learning_Engineering]]

# PyTorch
Using PyTorch we can simply train and run models on a single GPU.

We need for this:
- NVIDIA GPU
- NVIDIA driver
- CUDA installed (some PyTorch packages already have CUDA)