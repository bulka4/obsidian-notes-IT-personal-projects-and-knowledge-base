Tags: [[_Backend_Engineering]]
#BackendEngineering 

# Introduction
A policy engine is a system that evaluates rules (policies) to decide whether an action is allowed or denied.

It takes as an input an information about who is doing what, on which resource, under what conditions, and it returns ALLOW or DENY.