Tags: [[__Machine_Learning]]

# Scikit-learn
The Scikit-learn is a Python library used for building ML models.
## Arguments
- **Min_impurity_decrease** - Trees does a split if that split causes decrease of impurity bigger then that value. The bigger that value is the less if probability of overfitting but model might be less accurate. 
