Tags: [[__DevOps]], [[__Infrastructure_Engineering]], [[_Docker]] 

# Introduction
Docker uses dockerd as a runtime for running containers. Docker is communicating with it.

Alternative is the containerd.
## Dockerd.sock
It is a socket just like containerd.sock explained previously.

#DevOps #InfrastructureEngineering #Docker 