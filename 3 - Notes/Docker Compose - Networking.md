Tags: [[__DevOps]], [[__Infrastructure_Engineering]], [[_Docker]] 

# Introduction
Docker Compose make networking between container’s simpler.

It assigns DNS names to every container we run using Docker Compose. This DNS name is a name of a service in a docker-compose file.

#DevOps #InfrastructureEngineering #Docker 