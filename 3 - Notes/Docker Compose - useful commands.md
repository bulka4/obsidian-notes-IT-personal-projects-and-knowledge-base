Tags: [[__DevOps]], [[__Infrastructure_Engineering]], [[_Docker]] 

# Builds an image

Docker compose build

# Run container

Docker compose up

This command also builds images if they were not built yet.