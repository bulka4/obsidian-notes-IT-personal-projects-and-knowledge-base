Tags: [[__Machine_Learning]]
#MachineLearning 

# Introduction
When preparing a dataset for training a model, it might be useful to remove features which are very little correlated with a target variable we want to predict.

Those features are most probably not useful when predicting a target variable.

We can calculate correlation like described here - [[Pearson Correlation]].