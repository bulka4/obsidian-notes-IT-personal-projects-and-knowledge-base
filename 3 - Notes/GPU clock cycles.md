Tags: [[_Computer_hardware]]

# Introduction
A cycle is the basic “tick” of the GPU’s clock, like a heartbeat. During each cycle, an SM ([[GPU SMs (Streaming Multiprocessors)|link]]) can send an instruction to its wraps (to read data from a memory, perform some operation on that data).

#ComputerHardware 