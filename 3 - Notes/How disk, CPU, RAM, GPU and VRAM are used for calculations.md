Tags: [[_Computer_hardware]]

# Introduction
CPU can:
- Perform calculations on data which is stored in:
	- Disk (slow)
	- RAM (fast)
- Transfer data between disk, RAM and VRAM

GPU can perform calculations only on data which is stored in VRAM associated with that GPU.

#ComputerHardware