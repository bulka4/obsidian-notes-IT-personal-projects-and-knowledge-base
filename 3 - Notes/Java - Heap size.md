Tags: [[_Java]] [[__Programming_languages]]
#Java #ProgrammingLanguages 

# Introduction
Heap size determines how much memory a Java process can take. When that limit is exceeded, Java process gets killed. 

It is used so the entire server doesn't crash when Java wants to get too much memory.