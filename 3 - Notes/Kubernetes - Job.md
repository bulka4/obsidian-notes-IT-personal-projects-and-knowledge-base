Tags: [[__Cloud]], [[__DevOps]], [[__Distributed_computing]], [[__Infrastructure_Engineering]]

# Introduction
Job is a resource for executing scripts which executes one or more tasks and then exits. That is scripts which doesn’t run all the time until we stop them.

For example when we want to run a script which performs some one time calculations in a database.

#Cloud #DevOps #DistributedComputing #DataEngineering 