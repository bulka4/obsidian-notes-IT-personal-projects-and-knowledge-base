Tags: [[__Infrastructure_Engineering]] [[_Linux & Bash]]
#InfrastructureEngineering #LinuxBash 

# OR operator
Logical OR operator is created using the `||` command. If the first command fails (returns exit code different than 0), then the second command will be executed:
```bash
command1 || command2
```