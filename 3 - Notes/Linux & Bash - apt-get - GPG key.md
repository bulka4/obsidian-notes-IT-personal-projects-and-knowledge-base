Tags: [[__Infrastructure_Engineering]] [[_Linux & Bash]]
#InfrastructureEngineering #LinuxBash 

# Introduction
The GPG keys are used to ensure that the packages which we install using `apt-get` are secure. When we want to install packages from a new repository we might need to import ints GPG key to our system so apt-get trusts packages from it.