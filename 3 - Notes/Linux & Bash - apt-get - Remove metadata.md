Tags: [[__Infrastructure_Engineering]] [[_Linux & Bash]]
#InfrastructureEngineering #LinuxBash 

# Introduction
When installing tools using `apt-get`, we can remove created metadata using the `rm -rf /var/lib/apt/lists/*` command. This can save a lot of disk space.