Tags: [[_Visual_Studio_solutions]]

# Introduction
To load a project / solution, we click on `file > open > project / solution` and we need to choose either `.sln` or `.csproj` file.

If a project doesn't want to load, we can try to right click on the project name in the right hand side panel and click at `reload project`.