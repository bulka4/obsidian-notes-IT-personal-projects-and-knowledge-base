Tags: [[_ML_model_serving]]
#MLModelServing 

# Introduction
ML model serving is a task of creating a long running service which listens to clients (other applications) requests and sends back model predictions as a response. So clients can send a request at any time get a response.

Such a service is for example a Rest API or MCP server.