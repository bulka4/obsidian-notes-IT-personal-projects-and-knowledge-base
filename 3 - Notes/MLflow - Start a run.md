Tags: [[__Machine_Learning_Engineering]] [[_MLflow]]
#MLEngineering #MLflow 

# Introduction
The `mlflow.start_run()` function used in the MLflow Python scirpt will:
- Start a new run if it was not started yet (if we run our script using the `python script.py` command)
- Attach our script to the current run where this script runs (if we run the script using the `mlflow run` command)