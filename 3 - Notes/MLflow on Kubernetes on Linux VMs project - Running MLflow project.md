Tags: [[_My_projects]] [[__Data_Engineering]]
#MyProjects #DataEngineering 

# Introduction
To run a MLflow project, we can prepare a pod where we run Python code like it is done in the `/home/azureadmin/k8s/mlflow_test.yaml` manifest which is being prepared in the `bash_scripts/vm1_configure.tftpl` bash script ([[MLflow on Kubernetes on Linux VMs project - Configuring VMs using bash scripts|link]]).