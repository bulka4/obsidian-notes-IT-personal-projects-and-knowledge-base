Tags: [[__Data_Engineering]]

# Introduction
Transactions allow us to test the result of a query without affecting tables in a database.

We can execute some query and then revert all the changes made by this query. For example we can modify an existing table and then revert those changes.

We can do it like that:
![[2 - Images/MS SQL/Screenshot 1.png]]

The ‘ROLLBACK’ command reverts all the changes we made.

#DataEngineering 