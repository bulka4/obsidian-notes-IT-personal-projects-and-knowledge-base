Tags: [[__Machine_Learning]]
#MachineLearning 

# Introduction
Early stopping is a technique used when training ML models iteratively, for example when using gradient descent ([[Gradient Descent - ML|link]]), to prevent overfitting.

In this method we stop the training process once we see that there is no a significant improvement in model's performance between training steps.