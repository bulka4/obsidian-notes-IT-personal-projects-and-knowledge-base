Tags: [[__Machine_Learning]]
#MachineLearning 

# Introduction
Extrapolation in machine learning means making predictions outside the range of data we trained on.

For example, when in the training dataset a target variable which we were predicting had values between 10 - 100 and later model tries to make a prediction where a real value of a target variable is 1000.