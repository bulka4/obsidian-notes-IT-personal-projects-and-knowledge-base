Tags: [[_Databases]]
#Databases  

# Introduction
NoSQL databases have flexible schema - they are good when data changes often.

That means that no schema migration is required when we add new fields. We just start using a new field in all new documents without modifying all the existing documents.

It is easier than in SQL - [[SQL databases - Schema flexibility]].