Tags: [[_Power_BI]]
#PowerBI 

# Introduction
To use new credentials, we need to go to `File > Options and settings > Data source settings` and choose either `Edit permissions` or `clear permissions`. If we use `clear permissions`, then we need to back to the Power Query Editor and we will be asked for credentials again.