Tags: [[__Machine_Learning_Engineering]]

# Introduction
When we run a single task multiple times by calling .remote(), or when we run multiple tasks, then:
- All those tasks’ calls run in parallel
- Ray can run different tasks calls on different servers

**Benefits:**
- Parallel task execution
- Distribution of tasks across multiple servers

#MLEngineering 