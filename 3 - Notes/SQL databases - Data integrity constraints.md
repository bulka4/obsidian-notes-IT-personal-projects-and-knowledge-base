Tags: [[_Databases]]
#Databases  

# Introduction
SQL databases has strong constraints, there is a lot of rules available, for example:
- Values can't be null
- Values must be unique
- Primary key - values must be both unique and not null
- Foreign key - values must exist in a specific column in another table
- Restrict allowed values
- Specify a default value
- Column type must be always the same