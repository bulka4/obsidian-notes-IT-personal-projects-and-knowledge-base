Tags: [[_Software_Engineering]]
#SoftwareEngineering 

# Introduction
Assertions are statements in tests that check whether the actual result matches the expected result.

If an assertion fails, the test fails.

Example:
```
assert calculate_sum(2, 3) == 5
```

Meaning:
> "I expect `calculate_sum(2, 3)` to return `5`."