Tags: [[_Software_Engineering]]
#SoftwareEngineering 

# Introduction
Transaction boundaries define the scope of a transaction, which operations are included in that transaction, i.e. which operations must succeed or fail together as one atomic unit.