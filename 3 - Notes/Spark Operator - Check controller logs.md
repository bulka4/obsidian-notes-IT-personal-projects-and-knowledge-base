Tags: [[__Data_Engineering]], [[__Distributed_computing]]
#DataEngineering #DistributedComputing 

# Introduction
When we create a `SparkApplication` resource and it doesn't work (Spark driver is not being created), then we can check logs in the `spark-operator-controller-xxx` pod about why Spark Operator couldn't create the driver pod.