Tags: [[_Probability]], [[__Mathematics]]
#Probability #Mathematics 

# Introduction
Variance measures how spread out are values of a random variable, that is what is an average difference between a value of a random variable and its mean.
# Definition
Variance of a random variable $X$ is defined as:
$$
\text{Var}(X) = \mathbb{E}[X - \mathbb{E}(X)]^2
$$
# Interpretation
Variance tells us how far from an average data points are on average (average squared distance of points from the average).
# Approximation
Variance is approximated with the value:
$$
\large \frac{1}{n} \sum_{i=1}^n(y_i - \bar{y})^2
$$