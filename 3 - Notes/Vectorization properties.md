Tags: [[_Algebra]], [[__Mathematics]]

# Introduction
This document describes on a high level vectorization properties and provides links to documents with more details about them.
# Matrix vectorization identity
Then we the following equations are true:
$$
\begin{align}
& \text{vec}(AXB^T) = (B \otimes A)\text{vec}(X) \\
& \text{vec}(AB) = \text{vec}(IAB) = (B^T \otimes I)\text{vec}(A) \\
& \text{vec}(AB) = \text{vec}(ABI) = (I \otimes A)\text{vec}(B) \\
\end{align}
$$
More information can be found here - [[Matrix vectorization identity]].

#Algebra #Mathematics 