Tags: [[__Data_Engineering]]

# Introduction
We can create files called schema.yml which are describing our models, there are defined data quality test, tables and columns descriptions.

In the models folder we can create a few subfolders, each containing different models, and then in each of those folders we can create a schema.yml file describing models in that folder:
![[2 - Images/dbt/Screenshot 2.png]]

#DataEngineering 